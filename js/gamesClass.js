 export class Games {
     constructor(codigo, nombre, categoria, descripcion) {
         this.codigo = codigo;
         this.nombre = nombre;
         this.categoria = categoria;
         this.descripcion = descripcion;

     }
 }